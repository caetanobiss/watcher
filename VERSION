[1.3.2] - 31/08/2026
- Correção de travamento na 2ª execução de testes: reset de `is_cancelled` na entrada do runner paralelo.
- Desativação automática do Spring (`DISABLE_SPRING=1`) e desacoplamento de entrada (`stdin=DEVNULL`) para evitar lockouts em execuções repetidas.
- Remoção correta de file descriptors PTY fechados (`active_fds.discard`).
- Parsing resiliente de SSE por linhas no front-end e envio imediato de heartbeat de inicialização.

[1.3.1] - 31/08/2026
- Cancelamento instantâneo de testes RSpec via I/O não-bloqueante (select em PTY master).
- Resolução dinâmica de escopos (services, models, etc.) em múltiplos caminhos (spec/services, spec/app/services, spec/unit/services, *_service_spec.rb).
- Banners visíveis de erro de inicialização e alerta no painel Web com botão de cópia de logs para a IA.

[1.3.0] - 31/08/2026
- Opção de configuração do diretório/path do projeto target em settings.json e UI.
- Centralização da resolução de caminhos em src/config.py.
- Log de contexto para assistentes de IA (AI_LOG.md).

[1.2.0] - 28/08/2026
- Correções de name resolution em specs e cancelamento de testes via SIGKILL.

[1.1.0] - 25/08/2026
- Suporte a múltiplos temas visuais (Dark, Light, Neon, Mechanicus).
- Dashboard em tempo real com estatísticas de cobertura de testes RSpec.
